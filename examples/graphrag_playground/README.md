# graphrag_playground

Demonstrates a minimal GraphRAG pattern:

- create graph entities and relations
- index node text with custom embedder
- retrieve top matches with graph neighborhood context

## Run

```bash
dart pub get
dart run
```
